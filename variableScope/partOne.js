// What will the following code log to the console and why? 

// the following code will print out 'undefined'
console.log(greeting);

/* the variable 'greeting' is declared with 'var', which means that the initialisation 
** virtually happens in the beginning of the code var greeting; with an implicit value of 'undefined'
*/
var greeting = 'Hello world!';